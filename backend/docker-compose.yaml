services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: docker-backend
    ports:
      - "3000:8080"
    environment:
      GEMINI_API_KEY: ${GEMINI_API_KEY}